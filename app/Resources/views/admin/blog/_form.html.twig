{{ form_start(form) }}
    {{ form_widget(form) }}

<input type="submit" value="{{ button_label|default('Видалити') }}"
       class="{{ button_css|default("btn btn-primary") }}"/>

<a href="{{ path('admin_post_index') }}" class="btn btn-primary">
    Назад до списку
</a>
{{ form_end(form) }}
